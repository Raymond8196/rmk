# RMK + Elink 的 Claude Code 配置

本目录包含用于标准化、高效开发工作流（Vibe Coding）的 Claude Code 配置和工具。

**[English Version](README.md)**

---

## 📁 目录结构

```
.claude/
├── README.md                 # 英文版说明
├── README-zh.md              # 本文件（中文版）
├── permissions.json          # 预批准的安全命令
├── validation.md             # 验证框架文档（英文）
├── validation-zh.md          # 验证框架文档（中文）
├── validate.sh               # 自动化验证脚本
└── skills/                   # 自定义技能（斜杠命令）
    ├── test-elink.md
    ├── build-examples.md
    ├── check-embedded.md
    └── commit-elink.md
```

## 🚀 快速开始

### 给 Claude

在本项目上开始工作时，Claude 应该：

1. **首先阅读 [CLAUDE.md](../CLAUDE.md)** - 项目标准和约定
2. **自由使用预批准的命令** - 来自 `permissions.json`
3. **代码更改后运行验证**: `./.claude/validate.sh <level>`
4. **使用自定义技能** 进行常见工作流程（见下文）

### 给用户

在本项目上与 Claude 合作时：

1. **参考 CLAUDE.md** - 当 Claude 犯错时
2. **使用验证脚本** - 检查你自己的更改
3. **调用技能** - 使用 `/skill-name` 进行自动化工作流程

## 🛠️ 自定义技能

### /test-elink
Elink 协议的综合测试
```
/test-elink          # 运行所有测试
/test-elink --verbose # 详细输出
```

### /build-examples
构建所有 RMK 示例以验证兼容性
```
/build-examples                    # 构建全部
/build-examples --target stm32h7   # 构建特定目标
```

### /check-embedded
验证 no_std 兼容性和嵌入式就绪性
```
/check-embedded
```

### /commit-elink
标准化提交工作流程（高频操作）
```
/commit-elink "Add feature X"
```

详细文档请参阅 [skills/](skills/) 目录。

## ✅ 验证框架

验证框架通过 5 个级别的检查确保代码质量：

### 验证级别

| 级别 | 名称 | 持续时间 | 何时运行 |
|------|------|---------|---------|
| 1 | 语法与格式 | < 10秒 | 每次代码更改后 |
| 2 | 功能正确性 | < 2分钟 | 提交前 |
| 3 | 嵌入式兼容性 | < 1分钟 | 协议更改时 |
| 4 | 性能基准 | < 5分钟 | 优化工作时 |
| 5 | 完整构建矩阵 | < 10分钟 | PR 提交前 |

### 使用方法

```bash
# 运行默认验证（级别 2）
./.claude/validate.sh

# 运行特定级别
./.claude/validate.sh 1  # 快速格式检查
./.claude/validate.sh 4  # 包括基准测试
./.claude/validate.sh 5  # 完整矩阵
```

完整文档请参阅 [validation-zh.md](validation-zh.md)。

## 🔐 权限

预批准的命令（无需确认）：

- **Cargo**: `build`, `check`, `test`, `fmt`, `clippy`, `clean`, `doc`
- **Git（只读）**: `status`, `diff`, `log`, `branch`, `show`, `ls-files`
- **文件操作**: `ls`, `cat`, `find`, `grep`

始终需要批准的危险命令：
- `rm -rf`, `git reset --hard`, `git push --force`, `cargo publish`, `dd`

完整列表请参阅 [permissions.json](permissions.json)。

## 📚 文档

### 项目文档
- **[CLAUDE.md](../CLAUDE.md)** - 主开发指南
  - 代码标准（Rust、嵌入式、RMK 特定）
  - Git 提交约定
  - 测试标准
  - 常见错误和禁止事项

- **[docs/elink/](../docs/elink/)** - Elink 协议文档
  - 协议概述和设计
  - 集成指南
  - 性能分析
  - FAQ 和故障排除

### 关键原则

1. **所有文档用英文** - 代码注释、提交、文档（核心文档提供中文版）
2. **对话可用中文或英文** - 根据用户偏好
3. **遵循常规提交** - `type(scope): subject`
4. **提交前验证** - 使用验证脚本
5. **模式出现时更新 CLAUDE.md** - 飞轮效应

## 🎯 工作流程最佳实践

基于 [Boris Cherny 的 13 条建议](https://x.com/bcherny/status/...)：

### 1. 多个 Claude Code 实例
为不同任务运行独立实例，启用系统通知

### 2. CLI + GUI 模式
使用 `&` 和 `--teleport` 在 CLI/GUI 之间无缝切换

### 3. 使用 Opus 4.5 思考模式
当前模型（Sonnet 4）速度快，但对复杂决策使用 Opus

### 4. 共享 CLAUDE.md（✅ 已实现）
团队维护共享标准，形成飞轮效应

### 5. 代码审查自动引用 CLAUDE.md（✅ 已实现）
AI 从 CLAUDE.md 中记录的过去错误中学习

### 6. 从计划模式开始（Shift+Tab 两次）
对于复杂任务，先计划后实现

### 7. 高频操作作为技能（✅ 已实现）
`/commit-elink`, `/test-elink` - Claude 自主调用

### 8. 使用子代理自动化（✅ 已实现）
将复杂工作流程委托给专门的代理

### 9. 使用 PostToolUse Hook（⏳ 计划中）
代码更改后自动格式化

### 10. 使用 /permissions（✅ 已实现）
预批准安全命令以避免中断

### 11. 配置 MCP（⏳ 计划中）
集成工具如 Slack、BigQuery、Sentry

### 12. 长任务：--permission-mode=dontAsk（⏳ 按需）
让 Claude 使用 ralph-wiggum 插件自主工作

### 13. 给 Claude 验证手段（✅ 已实现）
当 Claude 可以验证自己的工作时，质量提高 2-3 倍

## 🔄 持续改进

### 何时更新配置

- **CLAUDE.md**: 当 Claude 犯错或出现新模式时
- **Skills**: 当常见工作流程变得重复时
- **Validation**: 当发现新的失败模式时
- **Permissions**: 当识别新的安全命令时

### 维护计划

- **每周**: 审查验证日志
- **每月**: 更新 CLAUDE.md 和文档
- **每个 PR**: 确保所有验证通过

## 📊 项目状态

### 已完成的设置（2026-02-09）
- ✅ 具有综合标准的 CLAUDE.md
- ✅ 验证框架（5 个级别）
- ✅ 自定义技能（4 个技能）
- ✅ 权限配置
- ✅ 文档结构（docs/elink/）
- ✅ FAQ 和路线图
- ✅ 中英双语文档

### 下一步
1. 运行验证：`./.claude/validate.sh 2`
2. 测试技能：尝试 `/test-elink` 和 `/check-embedded`
3. 使用标准化工作流程开始开发
4. 学习新模式时更新 CLAUDE.md

## 🤝 贡献

为本项目贡献时：

1. 彻底阅读 [CLAUDE.md](../CLAUDE.md)
2. 遵循所有代码标准
3. 提交前运行验证
4. 用英文编写提交消息
5. 为新功能更新文档

## 📞 支持

- **问题**: 在 GitHub 上报告 bug
- **问题**: 查看 [docs/elink/faq-zh.md](../docs/elink/faq-zh.md)
- **标准**: 参考 [CLAUDE.md](../CLAUDE.md)

---

**准备好使用 Claude Code 进行高效的 Vibe Coding**

*最后更新: 2026-02-09*
